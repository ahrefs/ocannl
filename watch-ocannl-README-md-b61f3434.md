# Task Notes

- Round 1: Keep the README refresh scoped to onboarding and doc correctness. Add a practical
  quickstart, fix stale or broken links, and avoid retiming roadmap milestones in README without
  updating `ROADMAP.md` in the same change.
- Round 2: Verified the OCaml floor from `dune-project`, `arrayjit.opam`, and
  `neural_nets_lib.opam` (`>= 5.3.0`). Keep the quickstart. Incorporate the valid peer feedback to
  add `docs/precision_inference.md` to the learning path, but leave milestone rescheduling out to
  avoid doc drift.
