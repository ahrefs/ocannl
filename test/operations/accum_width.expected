the fp8 scalar rival-rendering values are pairwise distinct: true
the f16 scalar rival-rendering values are pairwise distinct: true
the default-policy f16 reduction takes the backend's declared residency (2048 + 1x8: wide 2056 on CPU, per-step 2048 on GPU): true
Fp16_wide widens the f16 reduction on this backend too (2048 + 1x8 gives 2056): true
Fp16_wide holds the f32 residency even under narrow_compute_f32=false (2048 + 1x8 gives 2056): true
a Vectorized f16 reduction under Fp16_wide + narrow_compute_f32=false declines the SIMD chains: true
the declined Vectorized f16 reduction keeps f32 residency (equals the once-narrowed wide reference): true
the serial f16 reduction under Fp16_wide + narrow_compute_f32=false equals the once-narrowed wide reference: true
under Fp16_wide the f16 naive matmul equals the once-narrowed wide-accumulation reference: true
the default-policy f16 matmul matches the wide reference exactly where the backend widens (CPU) and diverges where it keeps storage residency (GPU): true
bf16 naive matmul equals the once-narrowed wide-accumulation reference: true
the emitted serial k-loop narrows the accumulator once per cell, not per step: true
a merge-buffer read is not the accumulator's own cell (the update shape is recognized): true
Unroll-annotated bf16 reduction keeps the wide accumulator (equals the serial result): true
materialized-unroll bf16 reduction keeps the wide accumulator (equals the serial result): true
Partition segments share one accumulator scope (equals the unsplit serial result): true
a partition segment stays addressable by later schedule ops (unrolling a segment equals the serial result): true
two-axis bf16 reduction equals the once-narrowed wide-accumulation reference: true
materialized-unrolled INNER reduction axis equals the serial result (the scope hoists through the outer reduction loop): true
materialized-unrolled OUTER reduction axis equals the serial result: true
Unroll-annotated both reduction axes equals the serial result: true
BOTH reduction axes materialized sequentially keep the whole-nest accumulator (equals the serial result): true
Pad-guarded materialized unroll equals the serial result (the guard peels into the scope): true
inner loops stay reachable after an outer materializing unroll (annotating them equals the serial result): true
the nested vectorized reduction rendering fires: true
a vectorized inner reduction axis folds into the whole-nest wide accumulator (equals the serial result): true
a serialized nested Workgroup_reduce keeps the whole-nest accumulator (equals the serial result): true
adjacent accumulations into one cell keep their per-assignment narrowing (256 +1 +1 stays 256 at bf16): true
index-guarded bf16 reduction accumulates wide across the guard (256 +1x5 narrows once to 260): true
an index-guarded Where-form update (virtualization's guarded-read shape) keeps the wide accumulator (256 +1x5 narrows once to 260): true
a non-reduction recurrence through a pre-existing scope keeps its per-iteration narrowing (256 -0.5 -0.5 stays 256 at bf16): true
a mixed-operator scope is not a reduction and keeps its per-iteration narrowing (256 +1 *1 stays 256 at bf16): true
a widened scope's opening init keeps its own assignment's rounding (128 + 0.5 rounds before the x3 reduction): true
a Workgroup_reduce loop serialized on cc keeps the Serial accumulator width (equals the serial result): true
the non-divisible SIMD reduction rendering fires: true
the SIMD reduction folds its non-divisible tail into the wide total (equals the serial result): true
fp8 e5m2 reduction accumulates wide and narrows once (16 + 8x0.5 reaches 20, not 16): true
narrow_compute_f32=false recovers per-operator rounding: the result differs from the widened default: true
narrow_compute_f32=false recovers per-step fp8 narrowing (16 + 8x0.5 stays 16): true
narrow_compute_f32=false brings back the per-k-step narrowing in the k-loop: true
under Bf16_wide the bf16 naive matmul equals the once-narrowed wide-accumulation reference: true
under Bf16_wide + narrow_compute_f32=false the bf16 naive matmul still equals the wide reference: true
the default-policy bf16 matmul matches the wide reference exactly where the backend widens bf16 and diverges where it keeps storage residency: true
