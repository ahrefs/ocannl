(* What [exit_stream_teardown_child]'s stand-in prints when its teardown runs, and what
   [exit_stream_teardown] looks for: spelled once, in the module both executables link. *)
let teardown = "STAND-IN-TEARDOWN-RAN"
